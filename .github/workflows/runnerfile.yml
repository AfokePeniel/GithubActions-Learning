name: Self-hosted runner workflow

on: workflow_dispatch

jobs:
  build:
    runs-on: [self-hosted, Linux, X64, LGHAR]
    steps:
      - name: Checkout repository
        run: echo "Checkout to repository" >> file.txt
      

      
