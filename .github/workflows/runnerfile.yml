name: Self-hosted runner workflow

on: workflow_dispatch

jobs:
  build:
    runs-on: [self-hosted, Linux, X64, LGHR]
    steps:
      - name: Checkout repository
        run: echo "Checkout repository" >> file.txt
      

      
